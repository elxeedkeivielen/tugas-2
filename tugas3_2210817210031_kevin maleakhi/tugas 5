#include <stdio.h>
int main() {
    int sec,h,k,u,p;

    printf("Total seconds : ");
    scanf("%d", &sec);

h=(sec % 60);
k=(sec % 3600) / 60;
u=(sec % 86400) / 3600;
p=(sec % (86400 * 30) / 86400;
    
    printf("H:K:U:P - %d:%d:%d:%d\n",h,k,u,p);

    return 0;
}
